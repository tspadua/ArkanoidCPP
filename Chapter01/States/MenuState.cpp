#include "MenuState.h"

MenuState::MenuState() {};

void MenuState::update(EntityManager& entityManager, float deltaTime)
{

};

void MenuState::initialize(EntityManager& entityManager)
{

};

void MenuState::exit(EntityManager& entityManager)
{

};

GameState* MenuState::handleInput(EntityManager& entityManager, const Uint8* keystate)
{
    
};

